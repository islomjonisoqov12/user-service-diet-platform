package uz.dam.userservice.exceptions.enums;

public enum ExceptionType {
    ID_NOT_NULL,
    OBJECT_NOT_FOUND,
    UNIQUE_CONSTRAINT,
    JWT_INVALID,
    AUTHENTICATION_EXCEPTION,
    INVALID_SYNTAX,
    USER_INACTIVE,
    AUTHORITY_LIST_EMPTY,
    ACCESS_DENIED
}
